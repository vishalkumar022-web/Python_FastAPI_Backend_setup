# here in the .env file we can set environment variables for our application
# for example, we can set the PORT variable to specify which port our server should run on and all 
# other environment variables/Configurations that we want to use in our application can be set here as well

