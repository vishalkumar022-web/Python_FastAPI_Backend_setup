# In this repository file, we will define the functions that will interact with the database to perform the necessary operations on the user data. These functions will be called by the controller functions defined in the controller.py file. We will use SQLAlchemy to interact with the database and to perform CRUD operations on the user data. We will also define any necessary queries to retrieve specific user data from the database.